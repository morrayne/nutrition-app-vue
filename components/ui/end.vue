<template>
  <div class="end"></div>
</template>

<style scoped lang="scss">
.end {
  width: 100%;
  min-height: calc(3.5 * var(--newrem));
  margin-top: auto;
  border-radius: calc(3 * var(--newrem));
  border: solid 1px var(--ex-background);
  background: var(--sub-background);
  box-shadow: var(--box-shadow);
}
</style>
